# Local analysis artifacts — backtest output references real session
# filenames/paths from this machine; never publish.
report*.json
backtest_*.log
*.tgz

# Tooling state
__pycache__/
*.pyc
.pytest_cache/
config.local.json

# Serena workspace (local-only: project config + cache + memories)
.serena/
